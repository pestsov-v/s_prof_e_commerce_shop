const USER = "user";
const CLIENT = "client";
const MANAGER = "manager";
const MODERATOR = "moderator";
const ADMIN = "admin";

const role = {
  user: USER,
  client: CLIENT,
  manager: MANAGER,
  moderator: MODERATOR,
  admin: ADMIN,
};

module.exports = role;
